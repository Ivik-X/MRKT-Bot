services:
  mrkt-bot:
    build: .
    container_name: mrkt-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./tokens.txt:/app/tokens.txt
      - ./proxies.txt:/app/proxies.txt:ro
      - ./logs:/app/logs
      - ./data:/app/data

